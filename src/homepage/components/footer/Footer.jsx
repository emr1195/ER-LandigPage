import React from 'react'
import {SectionContainer} from '../common'

export const Footer = ({footerInfo}) => {
  console.log(footerInfo)
  return (
    <SectionContainer sxContainer={{border: '1px solid black'}}>
      Footer
    </SectionContainer>
  )
}
